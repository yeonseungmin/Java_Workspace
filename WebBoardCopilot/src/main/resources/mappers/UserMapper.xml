<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.webboard.mapper.UserMapper">
    <resultMap id="userResultMap" type="com.webboard.model.User">
        <id column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="nickname" property="nickname" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 회원가입 -->
    <insert id="insertUser" parameterType="com.webboard.model.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (username, password, nickname)
        VALUES (#{username}, #{password}, #{nickname})
    </insert>

    <!-- username으로 사용자 조회 -->
    <select id="selectUserByUsername" parameterType="String" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 사용자 ID로 조회 -->
    <select id="selectUserById" parameterType="Long" resultMap="userResultMap">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <!-- 사용자 정보 수정 -->
    <update id="updateUser" parameterType="com.webboard.model.User">
        UPDATE user SET nickname = #{nickname}, password = #{password}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="deleteUser" parameterType="Long">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>

    <!-- username 중복 체크 -->
    <select id="checkDuplicateUsername" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user WHERE username = #{username}
    </select>
</mapper>
